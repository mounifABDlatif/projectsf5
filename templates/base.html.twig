<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
        and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}

        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}

        {% endblock %}
    </head>
    <body>
        <div class="container">
            {% include "_menu.html.twig" %}

            {# Pour accéder aux meassages flash, j'utilise app.flashes #}
            {% for type, messages in app.flashes %}
                {% for msg in messages %}
                    <div class="alert alert-{{ type }}">{{ msg|raw }}</div>
                    {# raw permet de faire lire les balises html #}
                    
                {% endfor %}

            {% endfor %}

            <h1>Bienvenue à la bibliothèque</h1>
            
            {% block body %}
            <p>Bonjour {{ prenom|upper }}</p>


            
            {% endblock %}

        
            <footer>
                <p>Ceci est le footer</p>
            
            </footer>
        </div>
    </body>
</html>

{# COMMENTAIRE SUR TWIG #}

{#
    il ne doit jamais y avoir de balise PHP dans un fichier twig

    Une balise twig est délimté par {% %} au lieu de <>
    Entre les {% %} il y a le type de la balise
    Une balise est fermé par {% 'end' suivi du type de la balis %}


    - Pour afficher le contenu d'une variable on utilise {{ }}
    - Les noms des variables ne commencent pas par $
    - Les noms des variables correspondent aux indices de l'array passé en 2ème paramètre de la méthode 'render'
#}
